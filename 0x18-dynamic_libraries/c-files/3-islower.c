#include "main.h"

/**
 * _islower - function that checks for lowercase character
 * @c: a character argument
 * Return: returns 1 if condition holds or zero if otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
